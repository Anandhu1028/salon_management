<div class="report-column"><article class="report-block"><h2>Purchase Summary</h2><div class="report-stat-grid"><div class="report-stat"><span>Total Purchase</span><strong>₹{{ number_format($totalPurchase, 2) }}</strong></div><div class="report-stat"><span>Total Quantity</span><strong>{{ $totalQuantity }}</strong></div><div class="report-stat"><span>Purchase Transactions</span><strong>{{ $purchaseRows->count() }}</strong></div></div></article><article class="report-block"><h2>Purchase History</h2>@if($purchaseRows->isNotEmpty())<div style="overflow:auto"><table class="report-table"><thead><tr><th>Date</th><th>Product</th><th>Quantity</th><th>Price / Unit</th><th>Total</th></tr></thead><tbody>@foreach($purchaseRows as $purchase)<tr><td>{{ $purchase->purchase_date->format('d M Y') }}</td><td>{{ $purchase->product?->product_name ?? 'Deleted product' }}</td><td>{{ $purchase->quantity }}</td><td>₹{{ number_format($purchase->product?->price ?? 0, 2) }}</td><td>₹{{ number_format($purchase->total_amount, 2) }}</td></tr>@endforeach</tbody></table></div>@else<div class="report-empty">No purchase data available for this date range.</div>@endif</article></div>
